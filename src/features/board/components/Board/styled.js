import styled from "styled-components"

export const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  overflow-y: hidden;
  overflow-x: auto;
`
export const Container = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
  overflow-y: hidden;
  overflow-x: auto;
`
export const QuickCardEditor = styled.div`
  background: rgba(0, 0, 0, 0.6);
  bottom: 0;
  color: #fff;
  left: 0;
  position: fixed;
  right: 0;
  top: 0;
  z-index: 10;
`
